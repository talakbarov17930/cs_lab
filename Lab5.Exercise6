#include <stdio.h>

int main() {
    int n;

    printf("Enter size of square (1-10): ");
    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            printf("%d ", 1 + 2 * j);
        }
        printf("\n");
    }

    return 0;
}

