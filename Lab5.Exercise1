#include <stdio.h>

int main() {
    char text[1000];
    int length = 0;
    
    printf("Enter text: ");
    gets(text);
    
    while (text[length] != '\0') {
        length++;
    }
    
    printf("Length: %d\n", length);
    return 0;
}

