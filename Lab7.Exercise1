#include <stdio.h>

int isEven(int num) {
    return num % 2 == 0 ? 1 : 0;
}

int main() {
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);
    printf("%d is %s\n", num, isEven(num) ? "even" : "odd");
    return 0;
}

