#include <stdio.h>

int main() {
    char text[1000];
    
    printf("Enter text: ");
    gets(text);

    for (int i = 0; text[i] != '\0'; i++) {
        if (text[i] == '#') {
            text[i] = '_';
        }
    }

    printf("Modified text: %s\n", text);
    return 0;
}

